name: Build Android APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: lebazare-manager/package-lock.json

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install dependencies
        working-directory: lebazare-manager
        run: npm ci

      - name: Generate native project
        working-directory: lebazare-manager
        run: npx expo prebuild --clean --platform android

      - name: Build APK
        working-directory: lebazare-manager/android
        run: ./gradlew assembleRelease

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: LeBazare-Manager
          path: lebazare-manager/android/app/build/outputs/apk/release/app-release.apk
          retention-days: 90
